// Khởi tạo thư viện Animate On Scroll (AOS)
AOS.init({
    duration: 800,  // Thời gian diễn ra hiệu ứng (miligiây)
    easing: 'ease-in-out', // Kiểu hiệu ứng
    once: true,     // Chỉ chạy hiệu ứng 1 lần
    mirror: false   // Không chạy ngược lại khi cuộn lên
});